import * as LP from '../LPEngine/LPEngine.js';

export class objTriangle extends LP.LPGameObject {
    constructor() {
        super();
        this.init = () => {
        };

        this.update = (_delta) => {
        };

        this.draw = () => {
        };
    }
}